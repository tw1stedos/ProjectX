using UnityEngine;

public class Ball : MonoBehaviour
{
    private void OnCollisionEnter(Collision collision)
    {
        // Логика начисления очков удалена, так как она теперь обрабатывается в GameManager
    }
}